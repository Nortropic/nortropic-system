# Nortropic File Structure — Full Reference

Complete canonical tree for a Nortropic local-service site. Generated by `/nortropic-init` from PROJECT-BRIEF.md.

```
<client>-<trade>/
├── .claude/
│   └── settings.local.json        # Prettier hook + permission allowlist (from nortropic-init)
├── public/
│   ├── images/
│   │   ├── raw/                   # källmaterial (slot-id-prefix; brand__* ägs av brand.mjs) — normaliseras av prebuild, aldrig serverat
│   │   ├── ref/                   # referensutrymme, normaliserat — i .vercelignore; OG-bilden läser härifrån
│   │   ├── hero-01.avif           # behandlade varianter — skrivs ENDAST av scripts/treatment.mjs
│   │   └── proof-01.avif
│   ├── brand/
│   │   ├── logo.svg               # full logotyp (logo.png vid raster-degradering) — skrivs av brand.mjs
│   │   └── logo-mark.svg          # märket ensamt — <BrandMark> läser härifrån
│   ├── icon-192.png               # manifest, any maskable — skrivs av brand.mjs
│   └── icon-512.png
├── src/
│   ├── app/
│   │   ├── layout.tsx             # html lang="sv", header/footer, Vercel Analytics
│   │   ├── page.tsx               # Hem
│   │   ├── tjanster/
│   │   │   ├── page.tsx           # services index
│   │   │   └── [slug]/page.tsx    # generateStaticParams from content/services.ts
│   │   ├── omraden/                   # PAKETVILLKORAT (lokal-se) — byggs ENDAST vid belagt paket
│   │   │   ├── page.tsx           # service area index
│   │   │   └── [slug]/page.tsx    # generateStaticParams from content/areas.ts
│   │   ├── om-oss/page.tsx
│   │   ├── omdomen/page.tsx
│   │   ├── kontakt/page.tsx
│   │   ├── faq/page.tsx
│   │   ├── integritetspolicy/page.tsx
│   │   ├── actions/
│   │   │   └── lead.ts            # "use server" — Zod → sendLeadEmail (lib/send-lead.ts)
│   │   ├── api/
│   │   │   └── puls/route.ts      # pulsens sändvägskontroll — PULS_TOKEN ur env, 404 utan token, mottagare LEAD_TEST_TO ur env
│   │   ├── not-found.tsx          # Swedish 404 with phone + nav back
│   │   ├── error.tsx              # Swedish error page — ALWAYS shows phone number
│   │   ├── sitemap.ts             # from content/services.ts (+ areas.ts ENDAST vid belagt lokal-se-paket)
│   │   ├── robots.ts
│   │   ├── opengraph-image.tsx    # OG: ref-hero + logotyp + namn via ImageResponse (+ .alt.txt) — se nortropic-bild/behandling.md
│   │   ├── icon.svg               # märket, mörkt läge inbakat — skrivs av brand.mjs, taggarna via Next-konventionen
│   │   ├── favicon.ico            # 16+32+48, <10 kB — skrivs av brand.mjs
│   │   ├── apple-icon.png         # 180×180, inga förrundade hörn — skrivs av brand.mjs
│   │   └── manifest.webmanifest   # namn + temafärg ur tokens + de två manifest-ikonerna — skrivs av brand.mjs
│   ├── components/
│   │   ├── site/
│   │   │   ├── header.tsx         # sticky, PhoneLink + CTA
│   │   │   ├── footer.tsx         # NAP from business.ts, org.nr, areas, policy link
│   │   │   ├── hero.tsx
│   │   │   ├── quote-form.tsx     # "use client", posts to actions/lead.ts
│   │   │   ├── cta-banner.tsx     # closing CTA, every page
│   │   │   ├── phone-link.tsx     # tel: + conversion tracking
│   │   │   ├── floating-call.tsx  # mobile floating call button
│   │   │   ├── trust-row.tsx      # betyg, år, certifikat badges
│   │   │   ├── testimonials.tsx
│   │   │   ├── service-card.tsx
│   │   │   ├── area-list.tsx
│   │   │   ├── faq-accordion.tsx
│   │   │   └── schema-markup.tsx  # JSON-LD from content/* — lokala schemadelar (serviceArea) ENDAST vid belagt lokal-se-paket
│   │   └── ui/                    # shadcn/ui generated primitives
│   ├── content/
│   │   ├── business.ts            # SINGLE SOURCE: NAP, org.nr, öppettider, phone, email, socials, geo
│   │   ├── profile.ts             # KÄRNA · SITE QUALITY CONTRACT v2 (enda bäraren, ingen syskon-JSON): kalibrering ur briefens §7 — primaraktion, gate1Test, kvitton, schemaTyp, seoLage, juridikflaggor, rostregister, branschAntislop, motionNiva
│   │   ├── services.ts            # Service[]: slug, namn, beskrivning, priceHint, jour, rotRut, faq
│   │   ├── areas.ts               # PAKETVILLKORAT (lokal-se) — Area[]: slug, ort, kommun, intro, landmark-anchors. Byggs ENDAST när paketet är belagt; vid core-only är frånvaron korrekt, aldrig ett fynd
│   │   ├── testimonials.ts        # name, ort, text, rating, date, source
│   │   └── faq.ts
│   └── lib/
│       ├── utils.ts               # cn() etc (shadcn)
│       ├── send-lead.ts           # sändinfrastrukturen (Resend) — delas av actions/lead.ts + api/puls; mottagare alltid ur env
│       └── seo.ts                 # metadata builders per page type (from nortropic-seo-lokal templates)
├── .env.local.example             # RESEND_API_KEY=, LEAD_TO_EMAIL=, LEAD_FROM_EMAIL= (portföljkonstanten formular@nortropic.se), PULS_TOKEN=, LEAD_TEST_TO= (Nortropics adress, aldrig kundens)
├── next.config.ts
├── tsconfig.json                  # strict
├── package.json                   # pnpm only
└── README.md                      # client handover pointer
```

## Content type contracts

**Fältnamnsspråket är ENGELSKA — innehållet är SVENSKA (formbeslut 2026-07-29, AH17).** Fältnamn, nycklar och interfaces i `content/*.ts` skrivs på engelska (`address.street`, `postalCode`, `hours`); allt värdeinnehåll — copy, alt-texter, meta, GBP-underlag — är svenskt. Skälet: schema.org-mappningen är engelsk (`streetAddress`/`postalCode`/`addressLocality` läses 1:1 ur `address`) — svenska fältnamn tvingar in ett översättningssteg per bygge, och ett översättningssteg är en tyst felkälla. **Kontrakten nedan är NORMATIVA, inte exempel:** bygg exakt dessa fält, hitta aldrig på egna fältnamn eller interfaces.

```ts
// content/business.ts — must exactly match Google Företagsprofil
export const business = {
  name: "",            // legal + display name
  orgNr: "",
  phone: "",           // E.164: +46...
  phoneDisplay: "",    // "08-123 45 67"
  email: "",
  address: { street: "", postalCode: "", city: "", publik: false },
  //   postalCode: ALLTID "NNN NN" med mellanslag ("971 87") — identiskt i schema, footer och GBP-underlag;
  //   avvikande format mellan ytorna är en NAP-avvikelse av samma klass som publik-regeln förhindrar.
  //   publik: false = serviceområdesföretag — adressen UTELÄMNAS ur JSON-LD (areaServed bär geografin)
  //   och döljs i GBP; footer visar ort/serviceområde, aldrig gatuadress. true kräver besöksadress i briefen.
  hours: [ /* { days: "Mån–Fre", open: "07:00", close: "17:00" } */ ],
  emergency: false,     // jour dygnet runt?
  serviceAreas: [],     // slugs into areas.ts
  rating: { value: 0, count: 0, url: "" },   // Google
  certifications: [],   // { label, org, url? }
  fTax: true,
} as const
```

```ts
// content/services.ts
export type Service = {
  slug: string          // "avloppsrensning"
  name: string          // "Avloppsrensning"
  short: string         // card text, ≤120 chars
  description: string   // page intro
  priceHint?: string    // "Fast pris fr. 1 495 kr efter ROT"
  rotRut?: "ROT" | "RUT"
  emergency?: boolean
  faq: { q: string; a: string }[]
}
```

## Rules
- Pages import content, never hardcode business facts
- New service/area = one array entry → page, sitemap, schema, nav all update
- `error.tsx` and `not-found.tsx` always render the phone number — a broken page must still generate the lead
- `app/robots.ts`: AI-crawlers (`GPTBot`, `PerplexityBot`, `ClaudeBot`, `OAI-SearchBot`) TILLÅTS medvetet på skarp klient — kunden vill bli hittad, inte skydda innehåll; blockering gör kunden osynlig i ChatGPT/Perplexity/Claude-svar. Skriv valet som kommentar i filen. Undantag: TESTKLIENT — där är total blockering korrekt (hela sajten är noindex)
